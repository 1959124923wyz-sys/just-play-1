# just-play-1

一个最小可运行的文字冒险游戏示例。

## 运行游戏

```bash
python game.py
```

## 命令说明

- 输入 `1/2/3...`：选择对应选项
- 输入 `0`：显示帮助
- 输入 `9`：退出游戏
- 其它输入：提示“无效输入”并停留在当前节点

## 运行测试

```bash
python -m unittest
```

## 故事文件

剧情在 `story.json` 中定义，启动时由 `game.py` 读取并驱动流程。
